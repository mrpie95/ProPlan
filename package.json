{
  "name": "proplan",
  "version": "0.1.0",
  "private": true,
  "description": "Single-file Gantt/phase planner. The app is Carpati Timeline.html; this package.json hosts the test runner + build/inline script.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "node scripts/build.mjs",
    "tauri": "tauri",
    "app:dev": "tauri dev",
    "app:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@vitest/coverage-v8": "^1.6.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  }
}
